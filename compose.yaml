services:
  tourhythm-ai:
    container_name: tourhythm-ai
    build: .
    command: sh -c """
      alembic upgrade head &&
      uvicorn src.app:app --host 0.0.0.0 --port 9000
      """
    ports:
      - "9000:9000"
    env_file:
      - .env
    depends_on: # Start app after database and redis is ready
      - tourhythm-ai-db
      - tourhythm-ai-redis

  tourhythm-ai-optimizer:
    container_name: tourhythm-ai-optimizer
    build: .
    command: celery -A src.optimizer._celery worker --loglevel=info
    env_file:
      - .env
    depends_on: # Start optimizer after database and redis is ready
      - tourhythm-ai-db
      - tourhythm-ai-redis

  tourhythm-ai-db:
    container_name: tourhythm-ai-db
    image: postgres:15
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes: # persist data
      - tourhythm-ai-data:/var/lib/postgresql/data

  tourhythm-ai-redis:
    container_name: tourhythm-ai-redis
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes: # persist data
      - tourhythm-ai-cache:/data


volumes:
  tourhythm-ai-data:
  tourhythm-ai-cache:
